{
  "name": "herdr-drovr",
  "version": "0.4.7",
  "description": "Herdr plugin for driving the focused tab or pane anywhere with fzf while preserving live panes and agents.",
  "license": "MIT",
  "private": false,
  "type": "module",
  "engines": {
    "node": ">=23"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "npm run typecheck && node test.ts"
  },
  "keywords": [
    "herdr",
    "herdr-plugin",
    "terminal",
    "fzf"
  ],
  "devDependencies": {
    "@types/node": "^26.1.0",
    "typescript": "^6.0.3"
  }
}
