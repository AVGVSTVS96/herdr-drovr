{
  "name": "herdr-tab-mover",
  "version": "0.1.0",
  "description": "Herdr plugin for moving the focused tab to another workspace with fzf while preserving live panes and agents.",
  "license": "MIT",
  "private": false,
  "type": "module",
  "engines": {
    "node": ">=23"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "npm run typecheck && node test.ts"
  },
  "keywords": [
    "herdr",
    "herdr-plugin",
    "terminal",
    "fzf"
  ],
  "devDependencies": {
    "@types/node": "^26.1.0",
    "typescript": "^6.0.3"
  }
}
